const roomGetters = {
  data: (state) => state.data,
  image: (state) => state.image,
  message: (state) => state.message,
  isSuccess: (state) => state.isSuccess,
  isUpload: (state) => state.isUpload,
};

export default roomGetters;
