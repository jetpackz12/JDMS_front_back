const electricityBillingPaymentGetters = {
  data: (state) => state.data,
  message: (state) => state.message,
  isSuccess: (state) => state.isSuccess,
};

export default electricityBillingPaymentGetters;
